{
	"name": "buildbot-connect",
	"displayName": "Buildbo Connect",
	"description": "Provide connection with buildbot instance and allow to start manual builds",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.47.0"
	},
	"categories": [
		"Other"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": {
			"title": "Buildbot Connect",
			"properties": {
				"buildbot.URL": {
					"type": "string",
					"description": "Buildbot web interface URL"
				},
				"buildbot.userName": {
					"type": "string",
					"description": "If login is requires, specify the username. You will be prompted for password and it will be stored securely."
				},
				"buildbot.allowSelf-signedCertificate": {
					"type": "boolean",
					"default": false,
					"description": "Allow self-signed SSL certificate. This option is dangerous and it is better not to check it!"
				}
			}
		},
		"commands": [
			{
				"command": "buildbot-connect.openAllBuilders",
				"category": "Buildbot",
				"title": "Open All Builders in External Browser"
			},
			{
				"command": "buildbot-connect.openBuilder",
				"category": "Buildbot",
				"title": "Open Selected Builder in External Browser"
			},
			{
				"command": "buildbot-connect.forceBuild",
				"category": "Buildbot",
				"title": "Force a Build"
			}
		]
	},
	"activationEvents": [
		"onCommand:buildbot-connect.openAllBuilders",
		"onCommand:buildbot-connect.openBuilder",
		"onCommand:buildbot-connect.forceBuild"
	],
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "node ./out/test/runTest.js",
		"package": "vsce package",
		"publish": "vsce publish"
	},
	"devDependencies": {
		"@types/base-64": "^0.1.3",
		"@types/glob": "^7.1.1",
		"@types/mocha": "^7.0.2",
		"@types/node": "^13.11.0",
		"@types/node-fetch": "^2.5.7",
		"@types/vscode": "^1.47.0",
		"@typescript-eslint/eslint-plugin": "^2.30.0",
		"@typescript-eslint/parser": "^2.30.0",
		"eslint": "^6.8.0",
		"glob": "^7.1.6",
		"mocha": "^7.1.2",
		"typescript": "^3.8.3",
		"vscode-test": "^1.3.0"
	},
	"dependencies": {
		"base-64": "^0.1.0",
		"keytar": "^6.0.1",
		"node-fetch": "^2.6.0",
		"ts-md5": "^1.2.7"
	}
}
